#pragma once

namespace Farlor
{
    namespace Core
    {
        // Structure that contains all data required for a single frame
        struct FrameParams
        {
            float m_deltaTime;
        };
    }
}